# Dedicated dependencies for the crawl -> document -> chunk -> embed pipeline.
# Keep these out of the lightweight API image if ingestion runs separately.
crawl4ai>=0.6.0
pypdf>=5.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
beautifulsoup4>=4.12.0
httpx>=0.27.0
